# Boss Listers / Jardin's Outpost — Handoff (2026-08-25)

Real, verified state as of tonight. Every item below was actually checked — live API calls, real test runs, real builds — not assumed. Where something is unverified, it says so explicitly.

## Repo

- **GitHub:** `mjardin17/boss-listers-mvp`
- **Branch:** `merge-codex-rewrite` — this is the one with everything from tonight. Not yet merged to `main`.
- **Local path:** `C:\Users\jjard\claude\BossListers\`

## What's real and working right now

| Platform | Status | Proof |
|---|---|---|
| eBay | ✅ Live | Real OAuth token exchange succeeded against production `api.ebay.com`. Real offer already exists on the account (`243763349011`, Adrian Peterson card, $19.99, status `UNPUBLISHED` — one API call from being a live listing). |
| Etsy | ⚠️ App-level only | App credentials work (`ETSY_KEYSTRING`/`ETSY_SHARED_SECRET`). No shop connected yet — needs one OAuth click via "Connect Etsy" on the Channels page. |
| Card scanning | ✅ Working | Powered by `GEMINI_API_KEY` → `lib/cardIdentification.js`. |
| Supabase | ✅ Live | Real database, real tables, RLS working. |
| Video Studio | ✅ Core proven | Real Remotion+FFmpeg render pipeline. Real acceptance render passed (1080x1920, h264+aac, verified with ffprobe). Create Video button wired into inventory. Auth required on all routes. **One thing still needed: run the migration below.** |

## What's real code but needs a credential from you

| Platform | What exists | What's missing |
|---|---|---|
| Facebook Marketplace | Real Catalog API connector (`lib/channels/apiConnectors.js` → `FacebookConnector`), dry-run verified | `FB_ACCESS_TOKEN` + `FB_PAGE_ID` — get these at developers.facebook.com/tools/explorer, select the **Jardin's Outpost** Page, permissions: `pages_read_engagement`, `pages_manage_posts`, `catalog_management` |
| Bonanza | Real connector matching the actual Bonapitit API (rebuilt tonight — the first version assumed the wrong API shape entirely) | `BONANZA_DEV_ID` + `BONANZA_CERT_ID` (get at api.bonanza.com/accounts/new), then a `fetchToken` call + you approving via the URL it returns, for `BONANZA_ACCESS_TOKEN` |

All of these go in **`.env.local`** in this repo. Never in chat, never in a committed file.

## One thing YOU need to do: run this migration

The local Supabase CLI is blocked on this machine by a real Application Control security policy — not something to bypass. Run this directly instead:

1. Go to **supabase.com/dashboard** → your project → **SQL Editor**
2. Paste and run the contents of `supabase/migrations/0011_video_studio_projects.sql`

Until this runs, Video Studio's "Save" and "Render" buttons will fail (they now point at a real database table that doesn't exist yet). This is the last step to make Video Studio fully live.

## Your new store identity (Jardin's Outpost)

| Thing | Value |
|---|---|
| Facebook Page | Jardin's Outpost |
| Instagram | @jardinoutpost |
| Pinterest | Jardin's Outpost / jardinoutpost |
| Email | jardinsoutpost@gmail.com |

**Only the Facebook Page + email matter to Boss Listers right now** — Instagram and Pinterest aren't wired into any Boss Listers connector (Instagram's publisher code lives in the separate `video-bot-pipeline` repo, for posting video clips, not marketplace listings).

Keep separate: **Gods & Glory** (@godsandgloryai on Instagram/Facebook) — that's your content channel, not the store. Don't mix credentials or accounts between the two.

## Password vault

Bitwarden is set up (free tier). Run this once you have a fresh session to pre-create folders + empty login templates for everything above (name/website filled in, you just add username/password):

```powershell
$env:BW_SESSION = (& "$env:APPDATA\npm\bw.ps1" unlock --raw)
cd "C:\Users\jjard\claude\BossListers"
node scripts/bitwarden-setup.js
```

## Video Studio — what's done vs. still open

**Done, verified:**
- Real render pipeline (Remotion + FFmpeg) — proven with a real MP4 using actual inventory photos
- Scene editor UI, CTA fields, live render-status polling
- Create Video button in inventory (`components/dashboard/InventoryHealthPanel.tsx`)
- Auth required on every route
- Real, tenant-scoped database persistence (code done, needs the migration run — see above)

**Still open, not started:**
- Audio upload UI (backend supports it, no UI for it yet)
- Playwright test coverage
- The old 19 Bonanza tests were stale (wrong API shape) — already rewritten as 23 correct tests, this one's actually done

## Dead/decoy codebases — do not use these

| Path | Why dead |
|---|---|
| `claude/boss-listers-mvp/marketplace-integration/` | No real `.env`, entry point doesn't exist on disk |
| `claude/BOSS-LISTERS/` (capital) | Zero real network calls anywhere, archived on GitHub |
| `claude/video-bot-pipeline/boss-listers-ai/`, `boss-listers-vercel/`, `EMPIRE_WORKSPACE/BossListers/` | Not git repos, not developed |

**`C:\Users\jjard\claude\BossListers\`, branch `merge-codex-rewrite`, is the only real one.**
